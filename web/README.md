# Garmin Insight Web

React + Vite + Tailwind mobile-first PWA for Garmin Insight.

```bash
npm install
npm run dev
npm run build
```

The app defaults to `http://localhost:8000`. Change the API Base URL and sync token in Settings. In production, serve the Vite build from Vercel or Netlify and point it at the deployed backend.

